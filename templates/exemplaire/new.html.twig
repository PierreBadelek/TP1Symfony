{% extends 'base.html.twig' %}

{% block title %}LibraShelf | Nouvel Exemplaire{% endblock %}

{% block body %}
    <div class="container pt-3">
        <div class="row justify-content-center">
            <div class="col-md-8">
                <h1 class="mb-4">Créer un nouvel exemplaire</h1>

                <div class="card">
                    <div class="card-body">
                        {{ include('exemplaire/_form.html.twig') }}
                    </div>
                </div>

                <div class="mt-3">
                    <a class="btn btn-primary" href="{{ path('app_exemplaire_index') }}">Retour à la liste</a>
                </div>
            </div>
        </div>
    </div>
{% endblock %}
