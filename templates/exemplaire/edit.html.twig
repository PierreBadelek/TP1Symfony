{% extends 'base.html.twig' %}

{% block title %}LibraShelf | Édition Exemplaire{% endblock %}

{% block body %}
    <div class="container pt-3">
        <div class="row justify-content-center">
            <div class="col-md-8">
                <h1 class="mb-4">Modifier un exemplaire</h1>

                <div class="card">
                    <div class="card-body">
                        {{ include('exemplaire/_form.html.twig', {'button_label': 'Mettre à jour'}) }}
                    </div>
                </div>

                <div class="mt-3">
                    <a class="btn btn-primary" href="{{ path('app_exemplaire_index') }}">Retour à la liste</a>
                    {{ include('exemplaire/_delete_form.html.twig') }}
                </div>
            </div>
        </div>
    </div>
{% endblock %}
